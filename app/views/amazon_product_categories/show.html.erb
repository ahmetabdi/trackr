<div class="ui main text container">
  <h1><%= @amazon_product_category.name %></h1>

  <h2>Products</h2>
  <div>
    <% @amazon_product_category.amazon_products.each do |p| %>
      <%= link_to p.title, amazon_product_category_amazon_product_path(amazon_product_category_id: @amazon_product_category, id: p) %>
    <% end %>
  </div>
</div>
