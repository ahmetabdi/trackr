<% flash.each do |key, value| %>
  <div class="<%= flash_class(key) %>">
    <i class="close icon"></i>
    <div class="header">
      <%= key.camelize %>
    </div>
    <%= value %>
  </div>
<% end %>
